## 1. Specification

- [x] 1.1 Define the set-level queue behavior and non-goals.

## 2. Frontend Queue

- [x] 2.1 Add tests for the Creation Mode queue strip and enqueue behavior.
- [x] 2.2 Add the queue strip container to the Creation Mode output panel.
- [x] 2.3 Add client-side queue state, selected set handling, and serial execution on the existing generate endpoint.
- [x] 2.4 Update the primary button copy and disabled state so a second set can be queued while one is running.
- [x] 2.5 Preserve SKU preview cards in queued set snapshots.
- [x] 2.6 Preserve completed queue entries for switching until the page is refreshed.

## 3. Styling And Verification

- [x] 3.1 Style the queue strip for desktop and mobile layouts.
- [x] 3.2 Refine the queue strip into compact pill controls with clear queue labels.
- [x] 3.3 Run targeted layout tests, full test suite, Pages build, and browser verification.
